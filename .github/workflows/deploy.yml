name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # メインブランチにプッシュされたときに実行

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write # リポジトリへの書き込み（ブランチ作成・プッシュ）に必要
      pages: write # GitHub Pagesのデプロイに必要
      id-token: write # OIDC認証の取得に必要

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      # Node.jsとpnpmのセットアップをactions/setup-nodeに統合
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # pnpm の依存関係をキャッシュ
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
          # ⭐ これにより pnpm が自動的にインストールされ、PATHに追加されます

      - name: Install dependencies and Build
        # pnpm が正しく PATH に追加されているはず
        run: |
          pnpm install
          pnpm run build

      # GitHub Pagesのデプロイアクション
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Viteのビルド成果物ディレクトリを指定
          publish_dir: ./dist
          publish_branch: 'gh-pages'
